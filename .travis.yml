sudo: required
dist: trusty
language: node_js
node_js:
  - '7'
install:
  - npm install -g truffle
  - npm install -g ganache-cli
  - npm install
script:
  - ganache-cli -p 7545
  - truffle test
  - npm run coverage && cat coverage/lcov.info | coveralls